import { SliceuntilePipe } from './sliceuntile.pipe';

describe('SliceuntilePipe', () => {
  it('create an instance', () => {
    const pipe = new SliceuntilePipe();
    expect(pipe).toBeTruthy();
  });
});
