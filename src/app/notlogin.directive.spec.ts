import { NotloginDirective } from './notlogin.directive';

describe('NotloginDirective', () => {
  it('should create an instance', () => {
    const directive = new NotloginDirective();
    expect(directive).toBeTruthy();
  });
});
